import { makeStyles } from "@mui/styles";

//create characteristics of the class
export default makeStyles(()=>({
    appBar:{
        borderRadius: 15,
        margin: '30px 0',
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alightItems: 'center',
    }
}));